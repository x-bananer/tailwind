# Utility First Classes

Tailwind CSS is a utility-first CSS framework.
Styles are applied by combining small classes directly in HTML.
Instead of writing custom CSS for each component, the component is built from utility classes.
